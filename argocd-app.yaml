apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: terraform-helm-argocd-pipeline
  namespace: argocd
spec:
  project: default
  source:
    repoURL : "https://github.com/isirajieinnocent/terraform-helm-argocd-pipeline"
    targetRevision: HEAD
    path: C:\Users\isira\terraform-helm-argocd-pipeline\complete-devops-project-time-printer\values.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true